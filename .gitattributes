# Git LFS — large Hugging Face / PyTorch artifacts under the shipped classifier
# (Streamlit Cloud + GitHub size limits; tokenizer JSON stays normal Git.)
final_feedback_classifier/**/*.safetensors filter=lfs diff=lfs merge=lfs -text
final_feedback_classifier/**/*.bin filter=lfs diff=lfs merge=lfs -text
