# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
*.spec

# Virtual environments
.venv/
venv/
ENV/

# Secrets & local env
.env
.env.*
!.env.example

# --- Reflex / Streamlit runtime ---
.web/
.states/
uploaded_files/
*.db

# Reflex bundled assets cache
assets/external/

# --- Node / frontend ---
node_modules/
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# --- Generated ML / analysis outputs (re-run scripts to recreate) ---
embeddings.npy
embeddings_2d.npy
minority_patterns.csv
minority_patterns.json
*_detected.csv

# --- Trained model folders ---
# Model weights (*.safetensors, *.bin) are tracked via Git LFS (see .gitattributes).
# Intermediate training checkpoints are excluded — only the final root model matters.
final_feedback_classifier/checkpoint-*/
final_feedback_classifier_v2/
feedback_model/
catme_feedback_classifier/checkpoints/
catme_feedback_classifier/checkpoint-*/
professor_feedback_classifier/checkpoints/
professor_feedback_classifier/checkpoint-*/

# --- Large datasets (too big for git; re-download or copy from scratch) ---
courseEval.csv

# --- Zero-shot label caches (regenerated by training scripts) ---
zero_shot_cache_*.json
zero_shot_labels_cache.json

# --- Test/analysis output CSVs ---
professor_predictions.csv
*_predictions.csv

# --- OS & editors ---
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.log

# --- Jupyter ---
.ipynb_checkpoints/
